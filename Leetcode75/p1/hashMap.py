"""
A HashMap in Python is equivalent to a dictionary (dict). HashMaps are used to store key-value pairs, where each key is unique, and they are very efficient for operations like lookups, inserts, and deletions, all of which have an average time complexity of O(1).

"""

